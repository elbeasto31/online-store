@model Good
@{
    ViewData["Title"] = "Order";
}

@if (Model is null)
{
    <h2 style="margin-left: 30px;">Item does not exist</h2>
}
else
{

    <h2 style="margin-left: 30px;">Placing your order for @Model.good_name</h2>


    @*<form asp-action="order" asp-controller="buy" style="margin-left: 30px;">
        <input type="hidden" name="order_id" />
        <input type="hidden" name="login" value="@ViewBag.User.login" />
        <input type="hidden" name="good_id" value="@Model.good_id" />
        <div class="form-group col-md-5">
            <label for="comemnt">Please add a comment to your order:</label>
            <input class="form-control" name="comment" />
        </div>
        <div class="panel-body">
            <button type="submit" id="submit" class="btn btn-primary">Sumbit</button>
        </div>
    </form>*@







    <form asp-action="Order" asp-controller="Buy" asp-anti-forgery="true" style="margin-top:10px; margin-left:30px;">
        <div class="validation" asp-validation-summary="ModelOnly"></div>
        <div>
            <input type="hidden" name="order_id" />
            <input type="hidden" name="login" value="@ViewBag.User.login" />
            <input type="hidden" name="good_id" value="@Model.good_id" />
            <div class="form-group">
                <label for="comemnt">Please add a comment to your order:</label>
                <input type="text" name="comment" />
            </div>
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-outline-dark" />
            </div>
        </div>
    </form>
}